# Система мониторинга и анализа портфеля облигаций

Это CLI-приложение, предназначенное для автоматизации отслеживания ключевых метрик по облигациям в портфеле, уведомления о важных событиях (оферты, изменения рейтингов) и экспорта данных для анализа.

## Основные возможности

*   **Синхронизация портфеля:** Загрузка данных из TBank API и/или Excel-файлов.
*   **Каталог облигаций:** Ведение локальной базы данных со всеми известными облигациями и их параметрами, включая вечные облигации (`perpetual_flag`).
*   **Мониторинг метрик:** Отслеживание кредитных рейтингов, уровня листинга MOEX, уровня риска Tinkoff и расчетных купонов для флоатеров.
*   **Анализ ликвидности:** Оценка потенциальных потерь при выходе из позиции по рыночной цене на основе анализа стакана заявок. Ликвидность является биржевым свойством ISIN и одинакова для всех брокеров на MOEX.
*   **Уведомления:** Отправка оповещений в Telegram о приближающихся офертах и значимых изменениях метрик.
*   **Экспорт:** Выгрузка данных портфеля в форматах `xlsx`, `csv` или вывод в консоль.
*   **Генерация графиков:** Визуализация истории изменения рейтингов и ликвидности для облигаций.

## Установка и запуск

1.  **Склонируйте репозиторий:**
    ```bash
    git clone <URL вашего репозитория>
    cd portfolio
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python3 -m venv venv
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    Проект использует `pyproject.toml` для управления зависимостями. Установите их в режиме редактирования (`-e`), чтобы изменения в коде сразу же применялись без переустановки.
    ```bash
    pip install -e .
    ```

4.  **Настройте переменные окружения:**
    Скопируйте файл `env.example` в `.env` и заполните его своими данными.
    ```bash
    cp env.example .env
    ```
    Откройте `.env` в текстовом редакторе и укажите `INVEST_TOKEN` (токен от TBank API) и, при необходимости, `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.

## Работа с базой данных

Проект использует SQLite в качестве базы данных. Файл базы данных по умолчанию находится в `data/ratings.db`. Для прямого взаимодействия с базой данных (выполнения SQL-запросов) можно использовать стандартную утилиту командной строки `sqlite3`.

**Установка `sqlite3` (если не установлена):**
*   **macOS (через Homebrew):** `brew install sqlite`
*   **Debian/Ubuntu:** `sudo apt-get install sqlite3`

**Примеры использования:**

1.  **Подключиться к базе данных:**
    ```bash
    sqlite3 data/ratings.db
    ```

2.  **Выполнить команды в интерактивном режиме:**
    После подключения вы попадете в командную оболочку `sqlite3`. Вот несколько полезных команд:
    *   Показать все таблицы: `.tables`
    *   Показать структуру таблицы: `.schema portfolio_positions`
    *   Выйти из оболочки: `.exit` или `.quit`

3.  **Выполнить SQL-запрос:**
    ```sql
    -- Посмотреть все позиции в портфеле
    SELECT * FROM portfolio_positions;

    -- Найти облигацию по ISIN
    SELECT isin, name, figi, offer_date FROM bonds_catalog WHERE isin = 'RU000A1084K3';
    ```

4. ** show tables
    ```sql
    SELECT name FROM sqlite_master WHERE type='table';
    ```

## Основные команды

### 📖 Подробная документация команд
Полная документация по всем командам доступна в папке [doc/commands/](doc/commands/README.md).

### 🚀 Быстрый старт
```bash
# Обновить каталог облигаций
python3 main.py update-bonds

# Обновить рыночные цены облигаций
python3 main.py update-market-prices --limit 100

# Синхронизировать портфель
python3 main.py sync-portfolio

# Анализ потенциальных покупок
python3 main.py analyze-buy-candidates top --limit 10

# Проверить изменения в портфеле
python3 main.py check-changes
```

### 📊 Основные команды анализа

| Команда | Описание | Документация |
|---------|----------|--------------|
| `analyze-buy-candidates` | Анализ потенциальных покупок | [📖](doc/commands/analyze-buy-candidates.md) |
| `calculate-ytm` | Расчет доходности к погашению | [📖](doc/commands/calculate-ytm.md) |
| `calculate-monthly-profit` | Расчет ежемесячной прибыли | [📖](doc/commands/calculate-monthly-profit.md) |
| `check-changes` | Проверка изменений в портфеле | [📖](doc/commands/check-changes.md) |
| `check-offers` | Проверка приближающихся оферт | [📖](doc/commands/check-offers.md) |

### 🔄 Команды обновления данных

| Команда | Описание | Документация |
|---------|----------|--------------|
| `update-bonds` | Обновление каталога и рыночных цен | [📖](doc/commands/update-bonds.md) |
| `update-market-prices` | Обновление рыночных цен облигаций | [📖](doc/commands/update-market-prices.md) |
| `update-ratings` | Обновление рейтингов и мониторинга | [📖](doc/commands/update-ratings.md) |
| `sync-portfolio` | Синхронизация портфеля | [📖](doc/commands/sync-portfolio.md) |

### 📈 Команды экспорта

| Команда | Описание | Документация |
|---------|----------|--------------|
| `export-portfolio` | Экспорт портфеля | [📖](doc/commands/export-portfolio.md) |
| `export-bonds` | Экспорт каталога облигаций | [📖](doc/commands/export-bonds.md) |
| `generate-plots` | Генерация графиков | [📖](doc/commands/generate-plots.md) |

# Portfolio Scripts

Этот репозиторий содержит набор утилит для анализа, мониторинга и управления инвестиционным портфелем.

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <URL репозитория>
cd portfolio
```

### 2. Создание и активация виртуального окружения

```bash
# Создание
python3 -m venv venv

# Активация на macOS / Linux
source venv/bin/activate

# Активация на Windows
# .\venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка файлов конфигурации

Для работы скриптов необходимо настроить два файла:

#### `.env` (Переменные окружения)
Создайте файл `.env` в корне проекта (можно скопировать `env.example`) и укажите ваш токен для Tinkoff Invest API:

```
# Токен для Tinkoff Invest API
INVEST_TOKEN="t.xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

#### `config.yaml` (Основной конфигурационный файл)
Этот файл содержит все ключевые настройки: пути к файлам, параметры API, настройки уведомлений и список используемых модулей-адаптеров. Отредактируйте его под ваши нужды.

Особенно важен раздел `monitoring_adapters`, который определяет, какие данные будут отслеживаться командой `update_ratings`.

## Управление логированием

Уровень детализации логов, выводимых в консоль, можно легко настроить. Это полезно для отладки или, наоборот, для уменьшения количества "шума" при штатной работе.

### Уровни логирования

-   `DEBUG`: Максимально подробный вывод. Показывает все шаги, запросы к API, содержимое ответов. Полезен для поиска и устранения ошибок.
-   `INFO`: Стандартный уровень. Отображает ключевые этапы выполнения команд: запуск, успешное завершение, количество обработанных элементов.
-   `WARNING`: Показывает предупреждения о некритичных проблемах (например, не найден файл с ISIN-кодами, API не вернуло данные по одной бумаге из ста).
-   `ERROR`: Сообщает о серьезных ошибках, которые не позволили выполнить часть операции, но программа продолжает работать.
-   `CRITICAL`: Уведомляет о критических сбоях, которые, скорее всего, приведут к аварийному завершению программы.

### Как изменить уровень логирования

1.  Откройте файл `main.py`.
2.  Найдите в начале функции `main()` строку `setup_logging(level=logging.DEBUG)`.
3.  Замените `logging.DEBUG` на нужный вам уровень.

**Примеры:**
-   **Для обычного использования (минимум логов):**
    ```python
    setup_logging(level=logging.INFO)
    ```
-   **Для отладки (максимум логов):**
    ```python
    setup_logging(level=logging.DEBUG)
    ```

## Использование

Все операции выполняются через единую точку входа — `main.py`.

```bash
# Общий формат запуска
python3 main.py <команда> [аргументы]
```

Чтобы посмотреть список всех команд и их параметры, выполните:
```bash
python3 main.py --help
# или для конкретной команды
python3 main.py <команда> --help
```

---

### Workflow: Как это работает?

1.  **Синхронизируйте портфель (`sync-portfolio`):** Сначала вы загружаете состав своего портфеля из TBank API или Excel-файлов.
2.  **Обновите каталог (`update-bonds`):** Затем вы загружаете и обновляете данные с MOEX по всем доступным на рынке облигациям.
3.  **Обновите рыночные цены (`update-market-prices`):** Получите актуальные рыночные цены для корректного анализа доходности.
4.  **Запустите мониторинг (`update-ratings`, `check-offers`):** Теперь вы можете проверять свои бумаги на предмет рисков, изменений рейтингов и т.д.
5.  **Анализируйте (`analyze-buy-candidates`, `export-portfolio`, `generate-plots`):** Изучайте состав портфеля, ищите новые возможности и анализируйте историю изменений.

---

### 1. Управление портфелем и каталогом

Эти команды формируют основу данных, с которой работают все остальные утилиты.

#### `sync-portfolio` — Синхронизация вашего портфеля

**Назначение:** Это **фундаментальная команда**, которая собирает данные о ваших реальных позициях из разных источников (TBank API, Excel-файлы) и сохраняет их в локальную базу данных. Она является **источником правды** для всех остальных команд, работающих с вашим портфелем. На данный момент команда сфокусирована на **облигациях** и игнорирует другие типы инструментов.

**Логика:**
-   Подключается к TBank API и/или читает Excel-файл.
-   Фильтрует только облигации.
-   Обогащает данные (например, находит ISIN по тикеру для данных из Excel).
-   Сохраняет всё в единую таблицу `portfolio_positions` в базе данных.

```bash
# Загрузить позиции из TBank API
python3 main.py sync-portfolio --source tbank

# Загрузить позиции из Excel (путь нужно указать в config.yaml или аргументом)
python3 main.py sync-portfolio --source excel --excel_path path/to/your/file.xlsx
```

#### `export-portfolio` — Экспорт текущего портфеля

**Назначение:** Позволяет выгрузить и посмотреть состав вашего портфеля, который был ранее синхронизирован с помощью `sync-portfolio`.

```bash
# Показать портфель в консоли
python3 main.py export-portfolio --format console

# Сохранить портфель в Excel-файл
python3 main.py export-portfolio --format xls
```

#### `update-bonds` — Обновление каталога всех облигаций

**Назначение:** Создает и поддерживает в актуальном состоянии локальную базу данных **всех** рублевых облигаций, доступных на рынке. Эта база используется для обогащения данных и анализа.

**Логика:**
Команда работает в два этапа, которыми можно управлять с помощью флага `--source`:
1.  **Загрузка из TBank (`--source tbank`):** Загружается полный список облигаций из TBank API и в локальный каталог добавляются те бумаги, которых там еще нет.
2.  **Обогащение с MOEX (`--source moex`):** Для облигаций в каталоге, у которых не хватает данных (например, нет уровня листинга или ставки купона), запрашивается и обновляется информация с Московской Биржи.

По умолчанию (`--source all` или без флага) выполняются оба шага.

**Типы облигаций в каталоге:**
- **Обычные облигации** — с фиксированной датой погашения
- **Вечные облигации** (`perpetual_flag = 1`) — бессрочные облигации без даты погашения
- **Облигации с амортизацией** (`amortization_flag = 1`) — с постепенным погашением номинала
- **Флоатеры** (`floating_coupon_flag = 1`) — с плавающей купонной ставкой

**Примеры использования:**
```bash
# Запустить полное обновление (TBank + MOEX)
python3 main.py update-bonds

# Только добавить новые облигации из TBank
python3 main.py update-bonds --source tbank

# Только обновить существующие данные с MOEX
python3 main.py update-bonds --source moex

# Обновить все и вывести результат в консоль
python3 main.py update-bonds --output console
```

#### `update-market-prices` — Обновление рыночных цен облигаций

**Назначение:** Получает и обновляет актуальные рыночные цены облигаций через TBank API. Эта команда необходима для корректной работы анализа доходности и команд поиска облигаций для покупки.

**Логика:**
- Получает список облигаций из каталога, у которых отсутствуют рыночные цены
- Запрашивает цены через TBank API пакетами (по 50 облигаций за раз)
- Обновляет цены в базе данных для дальнейшего использования в анализе
- Автоматически находит FIGI для облигаций, у которых он отсутствует

**Особенности:**
- **Пакетная обработка:** Обновляет цены группами для оптимизации API запросов
- **Автоматический поиск FIGI:** Находит недостающие FIGI через TBank API
- **Обработка ошибок:** Корректно обрабатывает случаи, когда цена недоступна
- **Прогресс-индикация:** Показывает количество обновленных облигаций

**Примеры использования:**
```bash
# Обновить цены для 100 облигаций
python3 main.py update-market-prices --limit 100

# Обновить цены для всех облигаций (может занять время)
python3 main.py update-market-prices

# Обновить цену для конкретной облигации
python3 main.py update-market-prices --isin RU000A1084K3
```

**Важно:** После обновления цен команда `analyze-buy-candidates` сможет показывать корректные результаты анализа доходности.

#### `check-offers` — Комплексное уведомление о рисках оферты

**Назначение:** Автоматически проверяет облигации в вашем портфеле на наличие приближающихся оферт (дат, когда эмитент может выкупить облигацию) и отправляет уведомления в Telegram. Система предназначена для минимизации рисков, связанных с офертами, которые могут существенно повлиять на доходность.

**Логика:**
-   Анализирует таблицу `portfolio_positions` на наличие облигаций с офертами.
-   Сравнивает `offer_date` с текущей датой, чтобы определить, приближается ли оферта.
-   Отправляет уведомление в Telegram, если оферта находится в пределах заданного срока (например, ближайшие 30 дней, конфигурируется).
-   Дополнительно информирует о типе купона (постоянный, переменный) и текущем спреде для флоатеров, если такой расчет доступен.
-   Использует механизм предотвращения спама, сохраняя информацию об уже отправленных уведомлениях в `monitoring_checks`.

```bash
# Запустить проверку оферт и отправить уведомления
python3 main.py check-offers
```

#### `add-bond` — Точечное добавление облигации

**Назначение:** Добавляет одну конкретную облигацию в ваш локальный каталог по её ISIN. Полезно, если какой-то редкой бумаги не оказалось в общей выгрузке.

```bash
python3 main.py add-bond RU000A109SP5
```

---

### 2. Мониторинг рисков

Эти команды активно проверяют ваши бумаги на предмет различных рисков и событий.

#### `update-ratings` — Главный сторожевой пёс

**Назначение:** **Ключевая команда для мониторинга.** Она проверяет актуальные данные для облигаций из вашего портфеля (и списка `watchlist`) и **немедленно уведомляет в Telegram** о важных изменениях: падении кредитного рейтинга, снижении уровня листинга или изменении ставки купона.

**Логика:**
-   Берет ISIN-коды ваших бумаг.
-   В соответствии с настройками в `config.yaml`, опрашивает разные источники (MOEX, TBank, парсеры сайтов).
-   Сравнивает новые данные со старыми.
-   При обнаружении расхождений — сохраняет историю и отправляет алерт.

```bash
# Запустить проверку и получить уведомления об изменениях
python3 main.py update-ratings
```

#### `check-offers` — Контроль оферт

**Назначение:** Заранее предупреждает о приближающихся датах оферт для облигаций с плавающим купоном (флоатеров). Это позволяет вовремя принять решение по бумаге.

```bash
# Проверить ближайшие оферты
python3 main.py check-offers
```

#### `calculate-spread` — Автоматический расчет спредов

**Назначение:** Обеспечивает корректную работу мониторинга для облигаций с плавающим купоном (флоатеров). Команда автоматически находит в портфеле флоатеры, у которых не задан спред к базовой ставке (например, RUONIA), и рассчитывает его на основе последней купонной выплаты.

**Режимы работы:**
-   **Автоматический (без аргументов):** Проверяет все флоатеры в портфеле. Этот режим используется в `run_update.sh`.
-   **Ручной (с аргументом `--ticker`):** Рассчитывает спред для одной конкретной облигации.

```bash
# Запустить автоматический расчет для всего портфеля
python3 main.py calculate-spread

# Рассчитать спред для конкретного тикера
python3 main.py calculate-spread --ticker RU000A1084K3
```

#### `analyze-liquidity` — Анализ ликвидности облигаций

**Назначение:** Анализирует ликвидность облигаций на основе данных стакана заявок из TBank API. Рассчитывает потенциальные потери при выходе из позиции по рыночной цене для заданного объёма (по умолчанию 60 000 руб.). **Учитывает торговое время** — предупреждает, если анализ проводится вне торговых сессий или когда инструмент недоступен для торговли.

**Особенности:**
- Анализ основан на реальных заявках в стакане на момент запроса
- Автоматическое определение торгового времени (основная сессия 10:00-18:50, вечерняя 19:05-23:50 МСК)
- Проверка доступности конкретного инструмента в текущую торговую сессию
- Контекстные предупреждения для корректной интерпретации результатов

**Режимы работы:**
- **Анализ одной облигации:** Указать конкретный ISIN
- **Анализ портфеля:** Проверить все облигации из портфеля (--all)
- **Анализ каталога:** Проверить все облигации из каталога (--all без портфеля)

```bash
# Анализ конкретной облигации
python3 main.py analyze-liquidity --isin RU000A105FZ9 --amount 60000

# Анализ всего портфеля с сортировкой по риску
python3 main.py analyze-liquidity --all --amount 100000

# Анализ с настройкой глубины стакана (1-20 уровней)
python3 main.py analyze-liquidity --isin RU000A105FZ9 --depth 10
```

**Интерпретация результатов:**
- **0-10%**: Низкий риск ликвидности ✅
- **10-20%**: Умеренный риск ликвидности ⚠️
- **20-50%**: Высокий риск ликвидности ⚠️
- **50%+**: Критический риск ликвидности ⚠️ (возможно, инструмент недоступен для торговли)

---

### 3. Аналитика и вспомогательные утилиты

#### `generate-plots` — Визуализация истории

**Назначение:** Генерирует интерактивные HTML-графики, которые показывают динамику различных метрик для ваших облигаций. Система автоматически строит графики для всех доступных типов данных.

**Типы создаваемых графиков:**
- **`rating_dynamics.html`** — История изменения кредитных рейтингов с цветовой шкалой
- **`listlevel_dynamics.html`** — Динамика уровней листинга MOEX (1-высший, 2-средний, 3-базовый)
- **`risk_dynamics.html`** — История уровня риска TBank (если включено отслеживание)
- **`liquidity_dynamics.html`** — Анализ ликвидности с пороговыми значениями и цветовой индикацией рисков

**Особенности графиков:**
- Интерактивность (zoom, hover, выбор данных)
- Автоматическая цветовая индикация рисков
- Пороговые линии для ключевых значений
- Временные метки и подробные всплывающие подсказки
- Адаптивный дизайн для просмотра на разных устройствах

```bash
# Построить все доступные графики
python3 main.py generate-plots
```

**Результат:** Графики сохраняются в директории `plots/` и открываются в любом браузере. Система анализирует ISIN-коды из файлов портфеля и списка наблюдения (настраиваются в `config.yaml`).

#### `check-changes` — Анализ изменений в консоли

**Назначение:** Похоже на `generate-plots`, но работает без графики. Выводит в консоль списки облигаций, у которых рейтинг или листинг когда-либо падал или рос. Работает только с локальными данными, не требует доступа в интернет.

```bash
python3 main.py check-changes
```

#### `export-bonds` — Продвинутый фильтр облигаций

**Назначение:** Позволяет отобрать облигации из **общего каталога** по сложным критериям (срок до погашения, ставка купона, частота выплат) и выгрузить их в CSV-файл. Отлично подходит для поиска новых инвестиционных идей.

```bash
# Найти все облигации с погашением в 2027-2029 гг. и купоном от 15%
python3 main.py export-bonds filtered_bonds.csv --min-maturity 2027-01-01 --max-maturity 2029-12-31 --min-coupon-rate 15.0
```

---

### 4. Служебные и тестовые команды

Эти команды нужны для обслуживания базы данных и отладки.

-   **`clear-data`**: Полностью очищает таблицы с историческими данными.
    -   `--table <имя_таблицы>`: Позволяет очистить только одну указанную таблицу, например `portfolio_positions`.
-   **`seed-data`**: Заполняет базу тестовыми данными (полезно для отладки).
-   **`check-db`**: Выполняет проверку целостности базы данных.
-   **`set-spread`**: Утилита для ручной установки спреда для флоатеров.
-   **`test-tbank`**, **`test-moex`** и др.: Набор утилит для прямой работы с API, минуя use-case'ы. Позволяют быстро проверить ответ от API или исследовать новую ручку.

### Все доступные команды

Ниже представлен полный список команд, доступных через `main.py`. Для получения детальной информации по аргументам конкретной команды используйте `python3 main.py <команда> --help`.

| Команда                | Описание                                                                    |
| ---------------------- | --------------------------------------------------------------------------- |
| `sync-portfolio`       | Синхронизирует позиции из разных источников (TBank, Excel) в локальную БД.   |
| `export-portfolio`     | Экспортирует текущий состав портфеля из БД в XLS или консоль.                |
| `update-bonds`         | Обновляет каталог облигаций (основную информацию) в локальной БД из MOEX API. |
| `update-market-prices` | Обновляет рыночные цены облигаций через TBank API.                        |
| `export-bonds`         | Экспортирует отфильтрованный список облигаций в CSV.                          |
| `add-bond`             | Добавляет одну облигацию в каталог по ISIN.                                 |
| `update-ratings`       | Обновляет отслеживаемые метрики для облигаций в портфеле (рейтинг, уровень листинга и т.д.). |
| `generate-plots`       | Генерирует HTML-графики по истории изменений.                          |
| `check-changes`        | Проверяет наличие значимых изменений в метриках и отправляет уведомление. |
| `check-offers`         | Проверяет наличие предстоящих оферт и амортизаций, отправляет уведомление. |
| `set-spread`           | Устанавливает спред к ставке RUONIA для облигации-флоатера. |
| `calculate-spread`     | Рассчитывает спред для флоатеров. Работает для одной облигации или автоматически для всего портфеля. |
| `analyze-liquidity`    | Анализирует ликвидность облигаций на основе стакана заявок, оценивает потенциальные потери при выходе из позиции. |
| `clear-data`           | Удаляет все данные из таблиц истории. Можно указать конкретную таблицу. |
| `seed-data`            | Заполняет таблицы истории тестовыми данными.                           |
| `test-moex`            | Выполняет тестовые запросы к MOEX API.                                 |
| `test-tbank`           | Выполняет тестовые запросы к TBank gRPC API.                         |
| `test-cbr`             | Выполняет тестовый запрос к API ЦБ РФ для получения ставки RUONIA.     |
| `test-rusbonds`        | Выполняет тестовые запросы к парсеру rusbonds.ru.                      |
| `test-smartlab`        | Выполняет тестовые запросы к парсеру smart-lab.ru.                     |
| `test-parser`          | Выполняет тестовые запросы к парсерам сайтов (агрегатор).            |
| `check-db`             | Выполняет проверку целостности базы данных.                            |
| `calculate-ytm`        | Анализ доходности облигаций для покупки и портфеля.                    |
| `calculate-monthly-profit` | Расчет ежемесячной прибыли по облигациям.                           |
| `analyze-buy-candidates` | Анализ облигаций для покупки (не в портфеле).                      |


### Служебные команды

#### `seed-data`
Заполняет базу данных начальными данными. ISIN-коды берутся из файла, указанного в `config.yaml`.

```bash
python3 main.py seed-data
```

#### `clear-data`
Полностью очищает все исторические данные и каталог. По умолчанию удаляет данные из всех отслеживаемых таблиц.

```bash
# Очистить все исторические данные
python3 main.py clear-data

# Очистить только таблицу с позициями портфеля
python3 main.py clear-data --table portfolio_positions
```

### Команды для тестирования API

Эти команды предназначены для прямой работы с API и могут быть полезны для отладки, проверки данных или исследования новых инструментов.

#### `test-tbank`
Набор инструментов для работы с TBank API.

-   **`find <запрос>`**: Ищет инструменты по тикеру, ISIN или названию.
-   **`get-by-figi <FIGI>`**: Получает детальную информацию по FIGI.
-   **`get-bond-coupons <FIGI>`**: Запрашивает и выводит график предстоящих купонных выплат.

#### `test-moex`
Набор инструментов для прямого взаимодействия с различными ручками MOEX ISS API. Полезно для отладки и исследования данных.

- **`description <ISIN>`**: Получает детальное описание ценной бумаги.
- **`marketdata <ISIN>`**: Запрашивает основные рыночные данные (цены, объемы).
- **`find <строка>`**: Ищет ценные бумаги по части тикера, ISIN или названия.
- **`find_raw <строка>`**: Аналогично `find`, но возвращает "сырой" JSON-ответ от API.
- **`full_spec <SECID>`**: Возвращает полную спецификацию по бумаге, включая все доступные поля.
- **`candles <SECID>`**: Получает исторические данные (свечи).
  - `--from_date`: Дата начала в формате `YYYY-MM-DD`.
  - `--interval`: Интервал (по умолчанию `24` для дневных свечей).
- **`engines`**: Выводит список всех доступных торговых систем (engines).
- **`markets <engine>`**: Показывает рынки для указанной торговой системы.
- **`rating <SECID>`**: Пытается получить кредитный рейтинг (требует платной подписки).


#### `test-smartlab`
Инструмент для тестирования парсера кредитных рейтингов с сайта `smart-lab.ru`.
```bash
python3 main.py test-smartlab RU000A107SA3
```

### Гибкая система мониторинга

Команда `update-ratings` использует модульную систему адаптеров для отслеживания различных метрик облигаций из портфеля. Вы можете гибко управлять, какие именно метрики проверять, редактируя файл `config.yaml`.

В секции `monitoring_adapters` перечислите названия адаптеров, которые должны быть активны:

```yaml
# config.yaml

monitoring_adapters:
  - moex_listlevel      # Уровень листинга на Московской Бирже
  - tinkoff.risk_level  # Уровень риска по классификации TBank
  - credit_rating       # Кредитный рейтинг (агрегатор)
  - moex_coupon         # Текущий купон (для флоатеров)
  - floater_coupon_calculator # Расчетная ставка купона для флоатеров (RUONIA + спред)
```

Чтобы отключить проверку, просто закомментируйте соответствующую строку.

---

## Автоматизация и планировщик задач (cron)

Для непрерывного мониторинга портфеля рекомендуется настроить автоматический запуск ключевых команд через cron или аналогичные планировщики задач.

### Готовый скрипт автоматизации

В проекте уже есть готовый скрипт `run_update.sh`, который выполняет полный цикл обновления и мониторинга:

```bash
#!/bin/bash
# Последовательность команд в run_update.sh:
# 1. calculate-spread    - Расчет спредов для новых флоатеров
# 2. update-ratings      - Обновление всех отслеживаемых метрик  
# 3. check-offers        - Проверка приближающихся оферт
```

**Что делает скрипт:**
- Активирует виртуальное окружение проекта
- Логирует все операции с временными метками в `cron.log`  
- Рассчитывает спреды для новых флоатеров в портфеле
- Обновляет все настроенные метрики (рейтинги, ликвидность, уровни листинга и т.д.)
- Проверяет приближающиеся оферты и отправляет уведомления
- Безопасно обрабатывает ошибки и продолжает выполнение

### Настройка cron

#### 1. Подготовка скрипта

**Важно:** Отредактируйте путь к проекту в `run_update.sh`:
```bash
# Замените на ваш реальный путь
PROJECT_DIR="/path/to/your/portfolio"
```

Сделайте скрипт исполняемым:
```bash
chmod +x run_update.sh
```

#### 2. Настройка расписания

Откройте crontab для редактирования:
```bash
crontab -e
```

**Формат записи cron:**
```
# Формат: минута час день_месяца месяц день_недели команда
# Поля:   0-59   0-23    1-31      1-12    0-7
# где день_недели: 0 или 7 = воскресенье, 1 = понедельник, ..., 6 = суббота
```

**Специальные символы:**
- `*` — любое значение (каждый)
- `,` — список значений (например, `1,3,5` = понедельник, среда, пятница)  
- `-` — диапазон значений (например, `1-5` = понедельник-пятница)
- `/` — шаг (например, `*/15` = каждые 15 минут)

**Дни недели для запоминания:**
- `0` или `7` = Воскресенье
- `1` = Понедельник  
- `2` = Вторник
- `3` = Среда
- `4` = Четверг
- `5` = Пятница
- `6` = Суббота

**Рекомендуемые варианты расписания:**

```bash
# Ежедневно в 9:00 утра (перед началом торгов)
# ↓ мин час день мес дн_нед
0 9 * * * /path/to/your/portfolio/run_update.sh

# Каждые 4 часа в рабочие дни (пн-пт)
# ↓ мин час (несколько значений) любой_день любой_месяц пн-пт
0 8,12,16,20 * * 1-5 /path/to/your/portfolio/run_update.sh

# Только в рабочие дни в 8:30 утра (оптимальный вариант)
# ↓ 30мин 8час любой_день любой_месяц пн-пт  
30 8 * * 1-5 /path/to/your/portfolio/run_update.sh

# Два раза в день: утром и вечером в будни
# Утром в 9:00
0 9 * * 1-5 /path/to/your/portfolio/run_update.sh
# Вечером в 18:00  
0 18 * * 1-5 /path/to/your/portfolio/run_update.sh

# Только по понедельникам в 10:00 (еженедельный мониторинг)
0 10 * * 1 /path/to/your/portfolio/run_update.sh
```

**Дополнительные примеры для понимания синтаксиса:**

```bash
# Каждые 15 минут (для тестирования)
*/15 * * * * /path/to/your/portfolio/run_update.sh

# Каждый час с 9 до 18 в рабочие дни
0 9-18 * * 1-5 /path/to/your/portfolio/run_update.sh

# Только 1 и 15 числа каждого месяца в 12:00
0 12 1,15 * * /path/to/your/portfolio/run_update.sh

# Только в первый рабочий день месяца в 8:00
0 8 1-7 * 1 /path/to/your/portfolio/run_update.sh
```

#### 3. Проверка работы

Посмотрите логи выполнения:
```bash
tail -f /path/to/your/portfolio/cron.log
```

Проверьте что cron-задача добавлена:
```bash
crontab -l
```

### Ручной запуск для тестирования

Перед настройкой автоматизации протестируйте скрипт вручную:

```bash
# Запуск из директории проекта
./run_update.sh

# Или полный путь
/path/to/your/portfolio/run_update.sh
```

### Мониторинг автоматизации

**Логирование:** Все операции записываются в `cron.log` с временными метками

**Структура лога:**
```
==============================================
Starting update job at Wed Jan 15 09:00:01 MSK 2025
==============================================
--- 2. Calculating spread for new floaters ---
[INFO] Найдено 2 флоатера в портфеле без спреда...
--- 3. Updating ratings ---
[INFO] Запуск мониторинга для 15 облигаций...
--- 4. Checking for upcoming floater offers ---
[INFO] Проверка оферт для 8 флоатеров...
Update job finished at Wed Jan 15 09:05:23 MSK 2025
```

**Уведомления:** Все важные изменения автоматически отправляются в Telegram (если настроено)

### Дополнительные возможности автоматизации

#### Генерация графиков по расписанию

Добавьте в cron еженедельную генерацию отчетов:
```bash
# Каждое воскресенье в 10:00 - генерация графиков
# ↓ 0мин 10час любой_день любой_месяц воскресенье
0 10 * * 0 cd /path/to/your/portfolio && source venv/bin/activate && python3 main.py generate-plots >> cron.log 2>&1

# Альтернативно: каждую субботу в 20:00 (конец торговой недели)
# ↓ 0мин 20час любой_день любой_месяц суббота
0 20 * * 6 cd /path/to/your/portfolio && source venv/bin/activate && python3 main.py generate-plots >> cron.log 2>&1
```

#### Очистка старых логов

Добавьте ротацию логов:
```bash
# Каждый месяц очищаем старые логи (1 числа в 3:00 ночи)
# ↓ 0мин 3час 1число любой_месяц любой_день
0 3 1 * * cd /path/to/your/portfolio && mv cron.log cron.log.$(date +\%Y\%m\%d) && touch cron.log

# Еженедельная очистка (каждый понедельник в 2:00)
# ↓ 0мин 2час любой_день любой_месяц понедельник
0 2 * * 1 cd /path/to/your/portfolio && mv cron.log cron.log.$(date +\%Y\%m\%d) && touch cron.log
```

### Альтернативы cron

**На macOS:** Можно использовать `launchd` вместо cron
**На Windows:** Task Scheduler или PowerShell скрипты  
**На серверах:** systemd timers или Ansible для более сложных сценариев

### Troubleshooting

**Проблема:** Скрипт не запускается через cron
**Решение:** Проверьте абсолютные пути и переменные окружения

**Проблема:** Нет уведомлений в Telegram
**Решение:** Убедитесь что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` настроены в `.env`

**Проблема:** Ошибки с активацией venv
**Решение:** Проверьте путь к `venv/bin/activate` в скрипте

---

### 2. Поддержка брокера Alor (интеграция с вашим портфелем)

Система поддерживает автоматическую загрузку портфеля с брокера Alor наряду с TBank. Все позиции сохраняются в общую таблицу `portfolio_positions`.

**Архитектура ликвидности:** Ликвидность является биржевым свойством ISIN, а не свойством брокера. Стакан заявок одинаков для всех брокеров на MOEX, поэтому анализ ликвидности, выполненный для одного брокера, применим ко всем брокерам для данного ISIN.

#### Настройка переменных окружения для Alor

В файле `.env` должны быть указаны:

```
# Для Alor API (боевой контур)
ALOR_TOKEN="<ваш access token>"
ALOR_REFRESH_TOKEN="<ваш refresh token>"
ALOR_CLIENT_ID="<ваш client id>"
ALOR_CLIENT_SECRET="<ваш client secret>"
ALOR_LOGIN="<ваш торговый логин, например D88199>"
```

> **Внимание!** Не публикуйте эти токены в открытых репозиториях. Refresh token и client secret — это ваши ключи доступа к брокеру.

#### Синхронизация портфеля Alor

Для загрузки портфеля Alor используйте команду:

```bash
python3 main.py sync-portfolio --source alor
```

Для одновременной загрузки портфелей TBank и Alor (и объединения их в одну БД):

```bash
python3 main.py sync-portfolio --source all
```

Все позиции будут сохранены в одну таблицу, с указанием брокера в поле `broker_name`.

#### Проверка работы Alor API

Для теста соединения и получения портфеля Alor выполните:

```bash
python3 main.py test-alor --test-type portfolio
```

---

## Зависимости

В проекте используется библиотека для генерации календарей:
- `ics` — генерация .ics файлов (https://pypi.org/project/ics/)

Установите её вместе с остальными зависимостями:
```bash
pip install -r requirements.txt
# или если используете pyproject.toml:
pip install ics
```

## Генерация календаря оферт (.ics)

Теперь вы можете получить файл календаря с датами оферт по облигациям из вашего портфеля и импортировать его в Google/Apple/Outlook календарь.

**Возможности:**
- Генерация .ics файла с датами оферт всех облигаций портфеля
- Отправка файла в Telegram или сохранение на диск (по выбору)

**Примеры запуска:**

```bash
# Отправить .ics файл с датами оферт в Telegram
python3 main.py sync-offers-to-calendar --output-mode telegram

# Сохранить .ics файл на диск
python3 main.py sync-offers-to-calendar --output-mode file --output-path ./offers.ics
```

Файл можно импортировать в любой календарь (Google, Apple, Outlook и др.).

## Архитектура (обновлено)

- Вся бизнес-логика построена на принципе инверсии зависимостей (DIP): use cases и сервисы работают через абстрактные интерфейсы, что облегчает расширение, тестирование и интеграцию новых источников данных.
- Новые сервисы (например, генератор календаря) легко подключаются и переиспользуются в разных сценариях.

---

## Новые команды анализа доходности

### `calculate-ytm` - Расчет доходности к погашению

Анализ доходности облигаций для покупки и портфеля.

**Аргументы:**
- `--mode`: Режим анализа (`buy` - для покупки, `portfolio` - уже купленные)
- `--min-ytm`: Минимальная доходность для фильтрации (в процентах)
- `--max-risk`: Максимальный уровень риска (1-5)
- `--min-maturity`: Минимальная дата погашения (YYYY-MM-DD)
- `--max-maturity`: Максимальная дата погашения (YYYY-MM-DD)
- `--no-amortization`: Исключить облигации с амортизацией
- `--no-perpetual`: Исключить вечные облигации (без даты погашения)
- `--monthly-coupons`: Только облигации с ежемесячными купонами (12 раз в год)
- `--fixed-coupon`: Только облигации с фиксированным купоном
- `--floating-coupon`: Только облигации с плавающим купоном
- `--limit`: Максимальное количество результатов

**Примеры:**
```bash
# Анализ облигаций для покупки с фиксированным купоном
python3 main.py calculate-ytm --mode buy --fixed-coupon --limit 10

# Анализ портфеля с плавающим купоном
python3 main.py calculate-ytm --mode portfolio --floating-coupon --limit 10

# Анализ с фильтрами по риску и дате погашения
python3 main.py calculate-ytm --mode buy --max-risk 2 --min-maturity 2025-01-01 --limit 5

# Исключить вечные облигации из анализа
python3 main.py calculate-ytm --mode buy --no-perpetual --limit 10
```

**Формула расчета доходности:**
```
Доходность = (Купонная ставка × Номинал) / Цена покупки × 100%
```

**Важно:** Доходность рассчитывается по-разному в зависимости от режима:
- **`--mode portfolio`** → доходность от вашей средней цены покупки
- **`--mode buy`** → доходность от текущей рыночной цены

Подробные SQL примеры и объяснения см. в [документации YTM](doc/ytm_sql_examples.md).

**Особенности:**
- **Корректно учитывает флоатеры** - использует рассчитанные купонные ставки из `monitoring_checks`
- **Колонка "Купон/мес"** - показывает общую сумму купонных выплат в месяц по всем бумагам в строке
- **Автоматический расчет** - для флоатеров купонная ставка = RUONIA + спред
- **Фильтрация по типу** - можно выбрать только фиксированные или только плавающие купоны
- **Поддержка вечных облигаций** - система корректно обрабатывает бессрочные облигации (`perpetual_flag = 1`)

### `calculate-monthly-profit` - Расчет ежемесячной прибыли

Расчет ежемесячной прибыли по облигациям с разбивкой на компоненты.

**Аргументы:**
- `--mode`: Режим отображения:
  - `summary` - сводка по всему портфелю
  - `detailed` - детальная прибыль по каждой бумаге
  - `by-broker` - прибыль по брокерам
  - `by-risk` - прибыль по уровням риска
  - `top-10` - топ облигаций по прибыли
- `--limit`: Максимальное количество результатов
- `--min-profit`: Минимальная ежемесячная прибыль для фильтрации

**Компоненты ежемесячной прибыли:**
1. **Купонный доход** - основной стабильный источник дохода
2. **Прибыль от изменения цены** - нереализованная прибыль/убыток
3. **Общая прибыль** = Купонный доход + Прибыль от цены

**Примеры:**
```bash
# Сводка по портфелю
python3 main.py calculate-monthly-profit --mode summary

# Топ-10 облигаций по прибыли
python3 main.py calculate-monthly-profit --mode top-10 --limit 10

# Детальный анализ с минимальной прибылью 1000₽
python3 main.py calculate-monthly-profit --mode detailed --min-profit 1000 --limit 20

# Анализ по брокерам
python3 main.py calculate-monthly-profit --mode by-broker

# Анализ по уровням риска
python3 main.py calculate-monthly-profit --mode by-risk
```

**Формулы расчета:**
```
Купонный доход за месяц = (Купонная ставка × Номинал) / Количество выплат в год × Количество
Прибыль от цены = (Текущая цена - Средняя цена) × Количество
Общая прибыль = Купонный доход + Прибыль от цены
```

### `analyze-buy-candidates` - Анализ облигаций для покупки

Анализирует облигации, которых нет в портфеле, для принятия решений о покупке.

**Режимы:**
- `top` - Топ бумаг по доходности
- `floaters` - Анализ флоатеров для покупки  
- `compare` - Сравнение с текущим портфелем

**Примеры:**
```bash
# Топ-5 бумаг по доходности (риск <= 1, до погашения > 1 месяца)
python3 main.py analyze-buy-candidates top --limit 5 --min-risk 1 --min-months 1

# Флоатеры для покупки
python3 main.py analyze-buy-candidates floaters --limit 10

# Сравнение с портфелем (риск <= 2)
python3 main.py analyze-buy-candidates compare --limit 10 --min-risk 2
```

**Особенности:**
- Учитывает как обычные облигации, так и флоатеры
- Показывает потенциальную доходность и рекомендации
- Фильтрует по уровню риска и времени до погашения
- Сравнивает с средней доходностью текущего портфеля
- Поддерживает вечные облигации (бессрочные без даты погашения)

### `set-spread` - Установка спреда для флоатеров

Устанавливает спред для облигаций с плавающим купоном, что необходимо для корректного расчета их доходности.

```bash
# Установить спред для конкретной облигации
python3 main.py set-spread --isin RU000A108Y86 --spread 2.5

# Установить спред для всех флоатеров в портфеле
python3 main.py set-spread --all --default-spread 2.0
```

## SQL Примеры

Подробные SQL запросы для анализа доходности и прибыли:
- [Примеры SQL для YTM](doc/ytm_sql_examples.md)
- [Примеры SQL для ежемесячной прибыли](doc/monthly_profit_sql_examples.md)
